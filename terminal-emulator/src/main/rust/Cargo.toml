[package]
name = "termux-rust"
version = "0.1.0"
edition = "2021"

[lib]
name = "termux_rust"
crate-type = ["cdylib"]

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = false
