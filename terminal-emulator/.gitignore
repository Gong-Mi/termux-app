# Gitignore for terminal-emulator project
# Ignore compiled artifacts
src/main/rust-exec/target/
# NOTE: termux_exec_device_probe binaries are intentionally tracked in git
# as APK assets (src/main/assets/termux-probes/**). Do NOT ignore them.
# Backup files
*.bak
# IDE/editor files
*.swp
*.swo
# Cargo lock if not needed (keep if needed)
# Cargo.lock
