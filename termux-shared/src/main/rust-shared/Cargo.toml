[package]
name = "termux-shared-rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = { version = "0.21.1", default-features = false }
nix = { version = "0.27", features = ["socket", "uio", "process"] }
libc = "0.2"
log = "0.4"
android_logger = "0.13"
